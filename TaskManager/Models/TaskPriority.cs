using System;

namespace TaskManager.Models
{
    /// <summary>
    /// Represents the priority levels for tasks
    /// </summary>
    public enum TaskPriority
    {
        Low = 1,
        Normal = 2,
        High = 3,
        Critical = 4
    }
}
